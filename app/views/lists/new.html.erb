<div class="container">
  <div class="new-list">
    <%= simple_form_for (@list) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.submit "Submit list", class: "btn btn-primary" %>
    <% end %>
    <%= link_to "Back", lists_path %>
  </div>
</div>
